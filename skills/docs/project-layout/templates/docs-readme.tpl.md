> 状态：活文档
> 摘要：<项目名> 的文档地图 —— 按任务路由到对应文档。
> 何时读：不知道该打开哪个文档时。
> 依赖：无

# <项目名> 文档地图

先看这张表，按任务跳到需要的文档，不必全读；每个文档顶部都有 `状态 / 摘要 / 何时读`，扫头部再决定要不要读正文。

## 要做什么 → 读哪个

| 你的任务 | 打开 |
|---|---|
| 上手项目 / 遵守开发约定 | `CLAUDE.md` |
| 了解架构全貌 | `design/<架构总纲>.md` |
| 设计 / 修改某个模块 | `design/<模块>.md` |
| 追溯某个关键决策为什么这么定 | `adr/` |
| 了解某项选型的调研依据 | `research/` |
| 部署 / 发版 / CI / 分支操作 | `guides/` |

## 首次阅读顺序

1. `CLAUDE.md`
2. 本地图
3. <架构总纲>
4. 要动哪个模块，再读对应设计文档

分层、头部规范、命名规则见全局 skill `project-layout`（`~/.claude/skills/project-layout/SKILL.md`），不在此重复。
