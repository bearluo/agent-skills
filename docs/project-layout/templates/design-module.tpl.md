> 状态：草案
> 摘要：<一句话：本模块是什么。扫这句即可判断相关性>
> 何时读：<什么任务该打开本文>
> 依赖：<依赖的其他模块 / 接口 / 文档，或「无」>

# <模块名> 设计

## TL;DR

<3–5 行：解决什么问题、关键接口一瞥、给谁用。
让读者不读全文也能拿到要点。>

## 目标与边界

- 做什么，解决什么问题。
- **不做什么** —— 边界在哪，哪些相邻职责不归它。
- **首版故意砍掉的能力**，以及什么条件下再加。

## 对外接口

```
<精确签名 + 关键注释：前置条件、副作用、错误语义>
```

## 行为与数据流

<内部数据结构；关键流程分步；边界情况、并发/重入、失败与恢复。>

## 关键设计决策

| # | 问题 | 选项 | 选定 | 一句话理由 |
|---|---|---|---|---|
| 1 | | | | |

<重大到影响其他模块的决策，单独写 ADR，这里只留链接。>

## 可测性与测试计划

- **接缝**：外部依赖通过哪个接口注入、测试时替换成什么。
- **用例清单**：正常路径 / 边界 / 错误 / 并发，逐条列出。

## 待拍板

1. <需要用户决定的问题，定了就删掉这节>

---

## 变更记录

<需求或做法变了就加一行，不改上面正文的旧结论时也要加。写清「当时遇到了什么」——这一列是防止重踩同一个坑的。推翻了原决策的，另写 ADR，这里只留链接。>

| 日期 | 改了什么 | 为什么（当时遇到了什么） | ADR |
|---|---|---|---|
| YYYY-MM-DD | | | |

## 实现记录（完成后补）

- **与设计的偏差**：
- **测试结果**：
- **遗留问题**：
