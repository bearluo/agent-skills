# <项目名>

<一句话定位>。当前阶段：**<阶段名>**（<这个阶段要做什么>）。

<与其他相关仓库的关系，若有。写清是否共用代码、是否独立部署。>

## 技术栈

- <服务/包名>：<技术栈>
- <服务/包名>：<技术栈>

选型理由见 `docs/adr/`，别在会话里重新推导。

## 目录结构

```
<只列真实存在的目录，一行一句说明它放什么>
```

规则：

- **目录用到才建，不预先铺空壳。** 上面是约定不是待办。
- 实施计划、排查记录、checklist 写 `.scratch/`（已 gitignore），不进 `docs/`；进度用 GitLab issue。
- <项目特有的目录规则，比如某类文件必须放哪、什么不能进哪个目录>

## 文档规则

目录与文档的通用约定见全局 skill `project-layout`：往 `docs/` 写任何文件前先读 `~/.claude/skills/project-layout/SKILL.md` 的「零、规则速查」。本项目与通用约定不同的地方写在下面，**以这里为准**；加一条要用户拍板：

- <本项目特有的文档规则 / 对通用约定的偏离，一行说清是什么、为什么；没有就整节删掉，只留上面那句指针>

先看 `docs/README.md` 文档地图。

## 开发约定

- 主干 `<分支名>`，<合并方式>。
- <分支开发方式>
- <构建/测试/启动命令，以及必须注意的坑>

## 业务红线

<这些是违反了就是 bug 的约束，不是最佳实践建议。没有就删掉整节。>

1. **<红线>** <一句话说清为什么，或指向出处>

## 不要做的事

- <明确否决过的方向，附一句理由>
